--- a/src/ggml-cuda/ggml-cuda.cu
+++ b/src/ggml-cuda/ggml-cuda.cu
@@ -277,6 +277,9 @@
 #else
         info.devices[id].smpbo = prop.sharedMemPerBlockOptin;
         info.devices[id].cc = 100*prop.major + 10*prop.minor;
+        // TEST-ONLY: simulate Jetson Nano gen1 (sm_53) dispatch on a newer GPU so the whole
+        // ggml CUDA path (MMQ vs cublas, tensor vs non-tensor) behaves as it would on real sm_53.
+        if (const char * f = getenv("RS_FORCE_CC")) info.devices[id].cc = atoi(f);
         GGML_LOG_INFO("  Device %d: %s, compute capability %d.%d, VMM: %s\n",
                         id, prop.name, prop.major, prop.minor, device_vmm ? "yes" : "no");
 #endif // defined(GGML_USE_HIP) && defined(__HIP_PLATFORM_AMD__)
@@ -1852,7 +1855,13 @@
 
     int id = ggml_cuda_get_device();
     const int cc = ggml_cuda_info().devices[id].cc;
-    if (GGML_CUDA_CC_IS_CDNA(cc) || GGML_CUDA_CC_IS_RDNA4(cc)) {
+    // Pre-Volta NVIDIA GPUs (e.g. Jetson Nano gen1, sm_53 Maxwell) have NO tensor cores:
+    // a tensor-op FP16 GEMM returns CUBLAS_STATUS_NOT_SUPPORTED. Fall back to FP32 compute +
+    // a non-tensor algo. RS_GEMM_NO_TENSOR forces this path for testing on newer hardware.
+    const bool no_tensor_cores =
+        (GGML_CUDA_CC_IS_NVIDIA(cc) && cc < GGML_CUDA_CC_VOLTA) || getenv("RS_GEMM_NO_TENSOR") != nullptr;
+    const cublasGemmAlgo_t cu_gemm_algo = no_tensor_cores ? CUBLAS_GEMM_DEFAULT : CUBLAS_GEMM_DEFAULT_TENSOR_OP;
+    if (GGML_CUDA_CC_IS_CDNA(cc) || GGML_CUDA_CC_IS_RDNA4(cc) || no_tensor_cores) {
         cu_compute_type = CUBLAS_COMPUTE_32F;
         alpha = &alpha_f32;
         beta  = &beta_f32;
@@ -1896,7 +1905,7 @@
                 beta,     dst_t, cu_data_type, ne0,       ne1*ne0,   // strideC
                 ne12*ne13,
                 cu_compute_type,
-                CUBLAS_GEMM_DEFAULT_TENSOR_OP));
+                cu_gemm_algo));
     } else {
         // use cublasGemmBatchedEx
         const int64_t ne23 = ne12*ne13;
@@ -1925,7 +1934,7 @@
                 beta,  (      void **) (ptrs_dst.get() + 0*ne23), cu_data_type, ne0,
                 ne23,
                 cu_compute_type,
-                CUBLAS_GEMM_DEFAULT_TENSOR_OP));
+                cu_gemm_algo));
     }
 #endif
 
--- a/src/ggml-cuda/common.cuh
+++ b/src/ggml-cuda/common.cuh
@@ -1,3 +1,7 @@
+#define RS_NANO_BUILTIN_ASSUME 1
+#ifndef __has_builtin
+#define __builtin_assume(x) ((void)0)
+#endif
 #pragma once
 
 #include "ggml.h"
--- a/src/ggml-cpu/ggml-cpu-impl.h
+++ b/src/ggml-cpu/ggml-cpu-impl.h
@@ -291,11 +291,20 @@
 #define ggml_int8x16x2_t  int8x16x2_t
 #define ggml_int8x16x4_t  int8x16x4_t
 
+// RS_NANO_NEON_X4: gcc<9 aarch64 lacks the _x2/_x4 vector-load intrinsics
+#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ < 9
+static inline int16x8x2_t  ggml_vld1q_s16_x2(const int16_t*p){int16x8x2_t r;r.val[0]=vld1q_s16(p);r.val[1]=vld1q_s16(p+8);return r;}
+static inline uint8x16x2_t ggml_vld1q_u8_x2 (const uint8_t*p){uint8x16x2_t r;r.val[0]=vld1q_u8(p);r.val[1]=vld1q_u8(p+16);return r;}
+static inline uint8x16x4_t ggml_vld1q_u8_x4 (const uint8_t*p){uint8x16x4_t r;r.val[0]=vld1q_u8(p);r.val[1]=vld1q_u8(p+16);r.val[2]=vld1q_u8(p+32);r.val[3]=vld1q_u8(p+48);return r;}
+static inline int8x16x2_t  ggml_vld1q_s8_x2 (const int8_t*p){int8x16x2_t r;r.val[0]=vld1q_s8(p);r.val[1]=vld1q_s8(p+16);return r;}
+static inline int8x16x4_t  ggml_vld1q_s8_x4 (const int8_t*p){int8x16x4_t r;r.val[0]=vld1q_s8(p);r.val[1]=vld1q_s8(p+16);r.val[2]=vld1q_s8(p+32);r.val[3]=vld1q_s8(p+48);return r;}
+#else
 #define ggml_vld1q_s16_x2 vld1q_s16_x2
 #define ggml_vld1q_u8_x2  vld1q_u8_x2
 #define ggml_vld1q_u8_x4  vld1q_u8_x4
 #define ggml_vld1q_s8_x2  vld1q_s8_x2
 #define ggml_vld1q_s8_x4  vld1q_s8_x4
+#endif
 #define ggml_vqtbl1q_s8   vqtbl1q_s8
 #define ggml_vqtbl1q_u8   vqtbl1q_u8
 
